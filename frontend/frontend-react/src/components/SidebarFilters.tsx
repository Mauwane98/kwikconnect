import React from 'react';

const SidebarFilters: React.FC<{ onApply?: (filters: any) => void }> = () => {
  // Filters UI removed per request — sidebar will render nothing.
  return null;
};

export default SidebarFilters;
